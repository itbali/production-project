import ArticleDetailPage from './ArticleDetailPage';

describe('ArticleDetailPage', () => {
    it(' should ', () => {
        const result = undefined;
        expect(result)
            .toBe(undefined);
    });
});

import ArticlesPage from './ArticlesPage';

describe('ArticlesPage', () => {
    it(' should ', () => {
        const result = undefined;
        expect(result)
            .toBe(undefined);
    });
});
